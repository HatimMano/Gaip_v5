const gameConfig = {
  snake: {
    cellSize: 35,
    gridSize: 10,
  },
  pong: {
    paddleHeight: 60,
    paddleWidth: 10,
    ballRadius: 8,
  },
  tango: {
    gridSize: 6,
    canvasSize: 400,          
    cellSize: 400 / 6,
  },
};

export default gameConfig;
